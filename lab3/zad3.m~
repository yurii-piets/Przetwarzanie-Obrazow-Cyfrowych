clearvars; close all; clc;

kolo = imread('kolo.bmp');
bKolo = boolean(kolo);

kwadrat = imread('kwarat');
bkwadrat = boolean(kwadrat);

figure(1);
imshow(bKolo);

figure(2);
imshow(bkwadrat);

nbKolo =~bKolo;
figure(
imshow(nbKolo);

imshow(nbJet);
andLenaJet = bLena & bJet;
orJetKolo = bLena | bJet;
xorLenaKolo = xor(bLena, bKolo);


